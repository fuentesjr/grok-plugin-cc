export function getSessionRuntimeStatus(_env = process.env, _cwd = process.cwd()) {
  // TODO(slice-2): Replace this importability stub with the ACP runtime implementation.
  return {
    mode: "direct",
    label: "direct startup",
    detail: "Grok ACP runtime integration is pending slice 2.",
    endpoint: null
  };
}
